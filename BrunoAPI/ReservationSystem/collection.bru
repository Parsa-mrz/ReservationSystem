auth {
  mode: bearer
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  baseUrl: http://localhost:8000/api/v1
  businessSlug: 
  authUrl: {{baseUrl}}/auth
  businessesUrl: {{baseUrl}}/businesses
  businessId: 
  servicesUrl: {{baseUrl}}/services
}
